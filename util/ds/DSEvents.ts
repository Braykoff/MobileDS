// These are here to prevent require cycles

/** Events emitted by the DSConnection's Event Emitter. */
export const DSEvents = {
  SocketConnectionChanged: "socketConnectionChanged",
  RobotStateChanged: "robotStateChanged"
}
